{
  "name": "@p2p-hub/smartbase",
  "version": "0.1.0",
  "description": "SmartBase: structured data with a safe, non-SQL structured query layer on the PBX/OLE standard",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@p2p-hub/core": "0.1.0",
    "@p2p-hub/sdk": "0.1.0"
  },
  "license": "MIT"
}
